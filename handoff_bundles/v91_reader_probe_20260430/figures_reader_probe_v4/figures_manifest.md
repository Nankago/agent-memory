# Reader Probe V4 Figures

- fig1_locomo_when_accuracy.png: LoCoMo `when` slice accuracy across runs.
- fig2_unknown_rate.png: Unknown rate by benchmark/model across runs.
- fig3_locomo_accuracy_exact.png: LoCoMo relaxed accuracy and exact match trends.
- fig4_locomo_json_rate.png: LoCoMo JSON object rate trends.

Run labels:
- v3: baseline reader probe v3.
- v4_qwen14_temporalfix_all: temporal fallback enabled for all benchmarks (Qwen14 only run).
- v4_qwen7_llama_locomo_only: temporal fallback enabled for LoCoMo only (Qwen7/Llama run).